[build]
  command = "npm run build"
  publish = "dist"

# SPA redirect — send all routes to index.html so React Router / client-side
# navigation works correctly on hard refresh or direct URL access.
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
